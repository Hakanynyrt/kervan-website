# Kervanheat.com — minimal _redirects
#
# Heavy legacy URL mapping (90+ rules) was rejected by Workers-with-Assets
# validator due to absolute URLs and non-allowed status codes (410).
# Reintroduce specific rules in this file once the Worker deploy pipeline
# is stable, OR (preferred) configure them at zone level via:
# Cloudflare Dashboard -> Rules -> Redirect Rules.
#
# Only relative paths and status 200/301/302/303/307/308 are allowed here.

/index              /tr/                         301
/home               /tr/                         301
/anasayfa           /tr/                         301
